// // will use zustand create for later

// import { create } from "zustand";

// const useDeleteModal = create((set) => ({
//   _id: "",
//   title: "",
//   onOpen: (_id: string, title: string) => set({ _id, title }),
//   onClose: () => set({ _id: "", title: "" }),
// }));

// export default useDeleteModal;
